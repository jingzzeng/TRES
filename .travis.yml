# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
warnings_are_errors: true
r:
  - oldrel
  - release
  - devel
r_github_packages: ManifoldOptim, MASS, methods, pracma, rTensor, stats

os:
 - linux
 - macos
